function note {
  cd $HOME/Dropbox/Notes/$1
}

function _note {
  local cur
  cur=${COMP_WORDS[COMP_CWORD]}
  COMPREPLY=( $( compgen -f $HOME/Dropbox/Notes/$cur  ) ) 
}

complete -o nospace -F _note note

