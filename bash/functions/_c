function c {
  cd $PROJECTS/$1
}

function _c {
  local cur
  cur=${COMP_WORDS[COMP_CWORD]}
  COMPREPLY=( $( compgen -d $PROJECTS/$cur |  perl -pi -e 's/.*\/(.*)/$1/g' ) ) 
}

complete -o nospace -F _c c

